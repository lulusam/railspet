<h1>New food</h1>

<%= form_for([@pet, @food]) do |f| %>
    <% if @food.errors.any? %>
        <div id="error_explanation">
            <h2><%= pluralize(@foods.errors.count, "error") %> prohibited this food from being saved:</h2>
            
            <ul>
                <% @food.errors.full_messages.each do [msg] %>
                    <li><%= msg %></li>
                <% end %>
            </ul>
        </div>
    <% end %>
    
    <div class="field">
        <%= f.label :name %><br>
        <%=f.text_field :name %>
    </div>
    <div class="field">
        <%= f.label :calories %><br>
        <%=f.text_field :calories %>
    </div>
    <div class="actions">
        <%= f.submit %>
    </div>
<% end %>

<%= link_to 'Back', pet_path(@pet) %>
